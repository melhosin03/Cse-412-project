run "npm install" under both frontend and backend to install node_modules

To start the website:
1. run "node server.js" under backend
2. run "npm start" under frontend


Change the basic pool information in server.js to connect your database
